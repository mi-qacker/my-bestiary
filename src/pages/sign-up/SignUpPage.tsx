import {Layout} from 'shared/ui/layout';
import {Header} from 'widgets/header';

export const SignUpPage = () => {
    return (
        <Layout>
            <Header/>
            <h1>SignUpPage</h1>
        </Layout>
    );
};