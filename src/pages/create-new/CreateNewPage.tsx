import {Layout} from 'shared/ui/layout';
import {Header} from 'widgets/header';

export const CreateNewPage = () => {
    return (
        <Layout>
            <Header/>
            <h1>CreateNewPage</h1>
        </Layout>
    );
};